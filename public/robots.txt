# https://sajivfrancis.com/robots.txt
#
# One portable group, honored identically by every major crawler
# (Googlebot; Bingbot — which also feeds DuckDuckGo, Yahoo & Ecosia; Yandex)
# and by AI crawlers (GPTBot, ClaudeBot, Google-Extended, PerplexityBot,
# CCBot). AI crawlers are intentionally allowed — all content here is public.
User-agent: *
Allow: /
Disallow: /admin/
Disallow: /pagefind/

# AI agent crawlers — explicitly allowed. Named groups are listed separately
# because a crawler that matches its own User-agent ignores the wildcard group,
# so the /admin and /pagefind exclusions are repeated here to stay in effect.
User-agent: GPTBot
Allow: /
Disallow: /admin/
Disallow: /pagefind/

User-agent: ClaudeBot
Allow: /
Disallow: /admin/
Disallow: /pagefind/

User-agent: PerplexityBot
Allow: /
Disallow: /admin/
Disallow: /pagefind/

Sitemap: https://sajivfrancis.com/sitemap-index.xml
