---
import BaseLayout from '@/layouts/BaseLayout.astro';
---

<BaseLayout
  title="About"
  description="About Sajiv Francis — Enterprise Architect leading AI programmes at a Fortune 100 technology company. Indo-Canadian living in Arizona."
  mainClass="max-w-6xl"
>
  <!-- Top section: image is a background on the wrapper, positioned
       top-right and bleeding to the viewport edge. The wrapper extends
       through Hero + Now + Background + Why so the image is visible
       (and the eye has something to follow) for the first ~1100-1300px
       of scroll. Below this wrapper, content goes full-width.

       This mirrors ianenders.com's editorial layout where the content
       area is a single tall div with the image positioned `top right`
       and text flowing in the left half. -->
  <div class="about-with-portrait">
    <section class="about-hero">
      <p class="about-eyebrow">About</p>
      <h1 class="about-name">Sajiv Francis</h1>
      <p class="about-role">
        Enterprise Architect — solution architecture, AI, digital
        transformations, and program delivery.
      </p>
      <p class="about-lede">
        Indo-Canadian, living in Arizona. I do this work inside a
        Fortune 100 technology company by day, and write here on the
        side about what changes when the practice of architecture
        meets new AI tools.
      </p>
    </section>

    <article class="prose dark:prose-invert max-w-none about-content-narrow">
      <h2>Now <span class="not-prose about-now-date">— May 2026</span></h2>
      <ul>
        <li>
          Trying to work out what agentic architectures actually look
          like in real enterprise stacks — beyond the demo videos.
        </li>
        <li>
          Building tooling for my own work in public: RAG over my notes,
          AI-grounded writing, automated white papers.
        </li>
        <li>
          Following SAP S/4HANA's evolution — especially where AI starts
          being useful inside it, not bolted on.
        </li>
      </ul>

      <h2>Background</h2>
      <p>
        Eight years of building at the intersection of AI and enterprise
        systems — from a 2018 grad-school location tool for Connecticut
        seniors, to an NLP-powered adaptive learning system in 2019
        (predating mainstream RAG by four years), to enterprise data
        orchestration, to leading AI programmes inside one
        of the world's largest technology companies.
      </p>

      <h2>Why this work</h2>
      <p>
        I came into enterprise architecture sideways — through digital transformation projects in SAP & software
        engineering, then through trying to make AI useful inside
        organisations. What I care about now is the
        seam where new model capabilities meet decades-old enterprise
        systems, and how architects translate one to the other without
        breaking either.
      </p>
    </article>

  </div>

  <article class="prose dark:prose-invert max-w-none about-content">
    <h2>Life outside work</h2>
    <p>Married, two kids. Most weekends are theirs.</p>
    <p>
      When time allows: I ride a Harley, collect stamps (a habit my
      grandfather used to do, decades ago, that I've never quite outgrown)
      and I also pretend to follow Formula 1 and MotoGP as closely as I
      once did.
    </p>

    <h2>Education &amp; communities</h2>
    <div class="not-prose about-edu-grid">
      <div>
        <h3 class="about-edu-heading">Education</h3>
        <ul class="about-edu-list">
          <li>M.S. Software Engineering — <a href="https://www.fairfield.edu" target="_blank" rel="noopener noreferrer">Fairfield University</a></li>
          <li>Applied Data Science — <a href="https://www.engineering.columbia.edu" target="_blank" rel="noopener noreferrer">Columbia Engineering</a></li>
          <li>AI-Driven Leadership — <a href="https://online.stanford.edu" target="_blank" rel="noopener noreferrer">Stanford</a></li>
        </ul>
      </div>
      <div>
        <h3 class="about-edu-heading">Communities</h3>
        <ul class="about-edu-list">
          <li>Association of Enterprise Architects (AEA)</li>
          <li>ASUG (Americas' SAP Users' Group)</li>
          <li>The Open Group — TOGAF certified member</li>
          <li>Arizona Science &amp; Engineering Fair (judge)</li>
        </ul>
      </div>
    </div>

    <h2>Writing</h2>
    <p>
      I write about the future of architecture practice at the
      intersection of AI and organisational design — for practitioners,
      not just academics. Recent pieces are on the
      <a href="/blog/">writing</a> page.
    </p>

    <section id="contact" class="not-prose about-contact-strip" aria-label="Keep in touch">
      <p class="about-contact-kicker">Keep in touch</p>
      <h2 class="about-contact-title">Stay in touch on writing and docs</h2>
      <p class="about-contact-copy">
        Share your email and I will follow up directly when relevant.
      </p>

      <form
        class="about-contact-form"
        id="about-contact-form"
        novalidate
      >
        <input
          class="about-contact-input"
          type="email"
          name="email"
          placeholder="you@company.com"
          autocomplete="email"
          required
          aria-label="Email address"
        />
        <input
          class="about-contact-honeypot"
          type="text"
          name="website"
          tabindex="-1"
          autocomplete="off"
          aria-hidden="true"
        />
        <button class="about-contact-btn" type="submit">Stay in touch</button>
      </form>

      <p id="about-contact-status" class="about-contact-status" aria-live="polite"></p>

      <p class="about-contact-meta">
        Prefer direct contact?
        <a href="mailto:connectsfrancis@gmail.com">connectsfrancis@gmail.com</a>
      </p>
    </section>
  </article>
</BaseLayout>

<style>
  /* ----------------------------------------------------------------------
     Hero — ianenders-style editorial layout.

     Desktop: a relative-positioned section that lets the cutout image
     bleed past the right page gutter to the viewport edge. The image is
     applied as a background on an absolutely-positioned element so it
     sits behind/beside the text without affecting flow.

     Mobile: the absolutely-positioned image is hidden; an <img> tag
     stacks above the text instead. Cleaner stacking and accessible alt.
     ---------------------------------------------------------------------- */
  /* Wrapper with portrait as a subtle underlay behind text.
     The image is intentionally desaturated and overlaid so text remains
     readable in both themes. */
  .about-with-portrait {
    position: relative;
    margin-bottom: 3rem;
    padding-top: 2rem;
    background-image: url('/img/authors/sajivfrancis-about.png');
    background-size: cover;
    background-position: center top;
    background-repeat: no-repeat;
    border-radius: 0.5rem;
    overflow: hidden;
  }
  .about-with-portrait::before {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      linear-gradient(
        to bottom,
        color-mix(in oklab, var(--color-bg) 82%, transparent),
        color-mix(in oklab, var(--color-bg) 90%, transparent)
      );
    z-index: 0;
  }
  .about-with-portrait::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(circle at 18% 16%, color-mix(in oklab, var(--color-bg) 86%, transparent), transparent 46%),
      repeating-linear-gradient(
        135deg,
        color-mix(in srgb, var(--color-border) 20%, transparent) 0,
        color-mix(in srgb, var(--color-border) 20%, transparent) 1px,
        transparent 1px,
        transparent 7px
      );
    opacity: 0.55;
    z-index: 0;
  }

  .about-hero {
    position: relative;
    z-index: 1;
    max-width: 36rem;
    padding-right: 1rem;
    margin-bottom: 2rem;
  }
  .about-eyebrow {
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-fg-muted);
    margin: 0 0 0.6rem;
    font-weight: 600;
  }
  .about-name {
    font-size: clamp(2.2rem, 4vw, 3.2rem);
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.05;
    margin: 0 0 0.6rem;
  }
  .about-role {
    font-size: clamp(1.05rem, 1.5vw, 1.25rem);
    color: var(--color-fg);
    margin: 0 0 1.2rem;
    line-height: 1.4;
  }
  .about-lede {
    font-size: 1rem;
    color: var(--color-fg-muted);
    line-height: 1.55;
    margin: 0;
    max-width: 28rem;
  }

  /* Content sections INSIDE the portrait wrapper need to stay in the
     left half so they don't overlap the image. Width tuned so prose
     ends well before the cutout's left edge. */
  .about-content-narrow {
    position: relative;
    z-index: 1;
    max-width: 36rem;
    padding-right: 1rem;
  }

  @media (max-width: 900px) {
    .about-with-portrait {
      padding-top: 0.5rem;
      background-size: 145%;
      background-position: center top;
    }
    .about-hero,
    .about-content-narrow {
      max-width: 100%;
      padding-right: 0;
    }
  }

  /* ----------------------------------------------------------------------
     Content section below the portrait wrapper — left-anchored so it
     sits in the same column as the prose above. No `margin: 0 auto`,
     because centering breaks the visual continuity with the upper
     left-aligned column. Slightly wider max-width (40rem vs 32rem)
     since there's no image competing for room.
     ---------------------------------------------------------------------- */
  .about-content {
    max-width: 40rem;
    margin: 0;
  }
  .about-now-date {
    font-size: 0.85rem;
    font-weight: 400;
    color: var(--color-fg-muted);
    margin-left: 0.4rem;
    vertical-align: middle;
  }

  /* Education + communities two-column grid */
  .about-edu-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem 2.5rem;
    margin: 0.8rem 0 1.5rem;
  }
  @media (max-width: 600px) {
    .about-edu-grid { grid-template-columns: 1fr; }
  }
  .about-edu-heading {
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 600;
    color: var(--color-fg-muted);
    margin: 0 0 0.5rem;
  }
  .about-edu-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    font-size: 0.95rem;
  }

  .about-contact-strip {
    margin-top: 2.2rem;
    border: 1px solid var(--color-border);
    border-radius: 0.75rem;
    padding: 1rem;
    background:
      radial-gradient(circle at 86% 12%, color-mix(in srgb, var(--color-accent) 16%, transparent) 0%, transparent 42%),
      linear-gradient(180deg, color-mix(in srgb, var(--color-bg) 92%, var(--color-accent) 8%) 0%, var(--color-bg) 100%);
  }
  .about-contact-kicker {
    margin: 0;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-fg-muted);
  }
  .about-contact-title {
    margin: 0.35rem 0 0;
    font-size: clamp(1.05rem, 1.8vw, 1.25rem);
    line-height: 1.25;
    letter-spacing: -0.01em;
  }
  .about-contact-copy {
    margin: 0.5rem 0 0;
    font-size: 0.92rem;
    line-height: 1.45;
    color: var(--color-fg-muted);
  }
  .about-contact-form {
    margin-top: 0.8rem;
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
  .about-contact-input {
    flex: 1;
    min-width: 220px;
    border: 1px solid var(--color-border-strong);
    border-radius: 0.5rem;
    background: var(--color-bg);
    color: var(--color-fg);
    font: inherit;
    font-size: 0.92rem;
    line-height: 1.2;
    padding: 0.52rem 0.68rem;
  }
  .about-contact-input:focus {
    outline: none;
    border-color: var(--color-accent);
  }
  .about-contact-btn {
    border: 1px solid var(--color-border-strong);
    border-radius: 999px;
    background: var(--color-bg-elevated, var(--color-bg));
    color: var(--color-fg);
    font: inherit;
    font-size: 0.82rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    line-height: 1;
    padding: 0.6rem 0.95rem;
    cursor: pointer;
    transition: color 160ms ease, border-color 160ms ease, transform 160ms ease;
  }
  .about-contact-btn:hover {
    color: var(--color-accent);
    border-color: var(--color-accent);
    transform: translateY(-1px);
  }
  .about-contact-btn:disabled {
    opacity: 0.65;
    cursor: not-allowed;
    transform: none;
  }
  .about-contact-honeypot {
    position: absolute;
    left: -9999px;
    width: 1px;
    height: 1px;
    opacity: 0;
    pointer-events: none;
  }
  .about-contact-status {
    margin: 0.55rem 0 0;
    min-height: 1.1rem;
    font-size: 0.78rem;
    color: var(--color-fg-muted);
  }
  .about-contact-status.is-error {
    color: var(--color-accent);
  }
  .about-contact-status.is-success {
    color: #2e7d32;
  }
  .about-contact-meta {
    margin: 0.6rem 0 0;
    font-size: 0.78rem;
    color: var(--color-fg-muted);
  }
</style>

<script is:inline>
  (() => {
    const form = document.getElementById('about-contact-form');
    if (!form) return;

    const statusEl = document.getElementById('about-contact-status');
    const emailInput = form.querySelector('input[name="email"]');
    const websiteInput = form.querySelector('input[name="website"]');
    const submitBtn = form.querySelector('.about-contact-btn');
    const endpoint = 'https://chat-worker.sfrancis2017.workers.dev/contact';

    const setStatus = (message, tone) => {
      if (!statusEl) return;
      statusEl.textContent = message;
      statusEl.classList.remove('is-error', 'is-success');
      if (tone) statusEl.classList.add(tone);
    };

    form.addEventListener('submit', async (event) => {
      event.preventDefault();
      const email = (emailInput?.value || '').trim();
      const website = (websiteInput?.value || '').trim();

      if (!email) {
        setStatus('Enter an email address first.', 'is-error');
        return;
      }

      setStatus('Submitting...');
      if (submitBtn) submitBtn.disabled = true;

      try {
        const response = await fetch(endpoint, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email, website }),
        });

        if (!response.ok) {
          const payload = await response.json().catch(() => ({}));
          throw new Error(payload.error || `HTTP ${response.status}`);
        }

        form.reset();
        setStatus('Thanks — received. I will follow up directly.', 'is-success');
      } catch (err) {
        setStatus('Could not submit right now. Please email connectsfrancis@gmail.com.', 'is-error');
        console.error('[about-contact] submit failed', err);
      } finally {
        if (submitBtn) submitBtn.disabled = false;
      }
    });
  })();
</script>
